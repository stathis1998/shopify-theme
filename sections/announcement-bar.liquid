<div class="p-2 bg-black text-white text-center full-width">{{ section.settings.announcement_text }}</div>

{% schema %}
{
  "name": "Announcement Bar",
  "settings": [
    {
      "id": "announcement_text",
      "type": "text",
      "label": "Announcement Text",
      "default": "Welcome to our store!"
    }
  ],
  "presets": [
    {
      "name": "Announcement Bar",
      "category": "Header",
      "settings": {
        "announcement_text": "Welcome to our store!"
      }
    }
  ]
}
{% endschema %}
